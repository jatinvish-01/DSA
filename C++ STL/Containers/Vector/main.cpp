#include <iostream>
#include <vector>
using namespace std;

int main(){

      //Vector creations
      vector<int> marks(5, 0);
      cout << *(marks.begin()); //0

      // vector<int> miles(10);

      // vector<int> distances(10, 0); // create 10 size of vector and element stores with 0.


      return 0;
}